const express = require("express");
const upload = require("../config/upload");

const {
  createSurat,
  getAllSurat,
} = require("../controllers/suratController");

const router = express.Router();

router.post("/", upload.single("file"), createSurat);

module.exports = router;
